apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: backend-team
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007 # This port can be any valid port within the NodePort range
  type: NodePort
